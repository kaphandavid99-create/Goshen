-- Run this in MySQL Workbench against Local instance MySQL97.
-- Query > New Query Tab, then the lightning bolt.

CREATE DATABASE IF NOT EXISTS goshen_provision
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE goshen_provision;
